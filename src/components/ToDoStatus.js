import React from "react";

// Choose which ToDoItems to display in ToDoList
export const ToDoStatus = () => {
	return (
		<div>
			<button>All</button>
			<button>Active</button>
			<button>Completed</button>
		</div>
	);
};
